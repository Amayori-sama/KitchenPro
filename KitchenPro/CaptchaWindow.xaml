<Window x:Class="KitchenPro.CaptchaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KitchenPro"
        mc:Ignorable="d"
        Title="CaptchaWindow" Height="200" Width="200"      
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <StackPanel HorizontalAlignment="Stretch" Margin="0,15,5,0">
            <Label Content="AbCde$" Width="Auto" FontWeight="Bold" HorizontalAlignment="Center" Name="CaptchaText" FontFamily="Segoe UI" FontSize="18" Foreground="#FF00B9FF"/>
            <Button Name="ResetCaptchaBtn" Margin="0,2,2,2" Width="100" Height="30" HorizontalContentAlignment="Center" Click="ResetCaptcha" Content="Reset"/>
            <TextBox x:Name="CaptchaTextBox" VerticalContentAlignment="Center" Padding="0,0,0,5" FontSize="16" TextAlignment="Center"/>
            <Button Name="ConfirmCaptcha" Margin="0,2,2,2" Width="100" Height="30" HorizontalContentAlignment="Center" Click="ConfirmCpt" Content="Проверить"/>
        </StackPanel>
    </Grid>
</Window>
